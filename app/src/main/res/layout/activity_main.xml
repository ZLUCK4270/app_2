<?xml version="1.0" encoding="utf-8"?>
<!-- ==========================================================
     ECOLIM MÓVIL - LAYOUT PRINCIPAL (activity_main.xml)
     DrawerLayout con CoordinatorLayout, Toolbar y BottomNavigationView
     ========================================================== -->
<androidx.drawerlayout.widget.DrawerLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/drawer_layout"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:fitsSystemWindows="true">

    <!-- Contenido Principal de la Pantalla -->
    <androidx.coordinatorlayout.widget.CoordinatorLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:background="@color/ecolim_surface">

        <!-- App Bar Superior -->
        <com.google.android.material.appbar.AppBarLayout
            android:id="@+id/app_bar_layout"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:background="@color/ecolim_surface"
            app:elevation="0dp">

            <com.google.android.material.appbar.MaterialToolbar
                android:id="@+id/toolbar"
                android:layout_width="match_parent"
                android:layout_height="?attr/actionBarSize"
                app:navigationIcon="@drawable/ic_menu"
                app:navigationIconTint="@color/ecolim_primary_dark"
                app:title="Ecolim"
                app:titleTextColor="@color/ecolim_primary_dark"
                app:subtitle="Gestión Ambiental"
                app:subtitleTextColor="@color/ecolim_text_muted" />

        </com.google.android.material.appbar.AppBarLayout>

        <!-- Contenedor de Fragments (Inicio, Registros, Sincronizar) -->
        <FrameLayout
            android:id="@+id/fragment_container"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:layout_marginBottom="64dp"
            app:layout_behavior="@string/appbar_scrolling_view_behavior" />

        <!-- Barra de Navegación Inferior -->
        <com.google.android.material.bottomnavigation.BottomNavigationView
            android:id="@+id/bottom_navigation"
            android:layout_width="match_parent"
            android:layout_height="64dp"
            android:layout_gravity="bottom"
            android:background="@color/ecolim_card_bg"
            app:elevation="8dp"
            app:itemIconTint="@color/ecolim_primary"
            app:itemTextColor="@color/ecolim_primary"
            app:menu="@menu/bottom_nav_menu" />

    </androidx.coordinatorlayout.widget.CoordinatorLayout>

    <!-- Menú Lateral (Drawer) de Sarah M. -->
    <com.google.android.material.navigation.NavigationView
        android:id="@+id/nav_view"
        android:layout_width="320dp"
        android:layout_height="match_parent"
        android:layout_gravity="start"
        android:fitsSystemWindows="true"
        android:background="@color/ecolim_card_bg"
        app:headerLayout="@layout/nav_header_tecnico"
        app:menu="@menu/drawer_menu" />

</androidx.drawerlayout.widget.DrawerLayout>
